# Browserbase (required for cloud browsers)
BROWSERBASE_PROJECT_ID=your_project_id
BROWSERBASE_API_KEY=your_api_key

# Shared AI config (same key can be used by orchestrator-workers)
AI_PROVIDER=openai
AI_API_KEY=your_ai_key
# AI_PROVIDER can be: openai | google | anthropic
